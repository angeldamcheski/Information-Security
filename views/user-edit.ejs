<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit User</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f5f5f5;
        color: #333;
      }
      h2 {
        text-align: center;
      }
      form {
        max-width: 400px;
        margin: 20px auto;
        padding: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        border-radius: 10px;
        background-color: #fff;
      }

      label {
        display: block;
        margin-bottom: 8px;
        color: #333;
      }

      input {
        width: 100%;
        padding: 8px;
        margin-bottom: 16px;
        box-sizing: border-box;
      }

      button {
        background-color: #2596be;
        color: #fff;
        padding: 10px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }
      body > form > div:nth-child(7) > label {
        margin-top: -35px;
      }
      body > form > div:nth-child(8) > label {
        margin-top: -35px;
      }
      button:hover {
        background-color: #1c7a9c;
      }
      #back-btn {
        margin-right: 0px;
      }
    </style>
  </head>
  <body>
    <form action="/edit/<%= editUser._id %>" method="POST">
      <h2>Edit User</h2>

      <label for="username">Username:</label>
      <input
        type="text"
        id="username"
        name="username"
        value="<%= editUser.name %>"
        required
      />

      <label for="email">Email:</label>
      <input
        type="email"
        id="email"
        name="email"
        value="<%= editUser.email %>"
        required
      />
      <label for="adminPrivileges">Give admin privileges?</label>
      <div>
        <input type="radio" id="yes" name="adminPrivileges" value="true" <%=
        editUser.isAdmin ? 'checked' : '' %> />
        <label for="yes">Yes</label>
      </div>
      <div>
        <input type="radio" id="no" name="adminPrivileges" value="false" <%=
        !editUser.isAdmin ? 'checked' : '' %> />
        <label for="no">No</label>
      </div>
      <!-- Add more fields as needed -->

      <button type="submit">Save Changes</button>
      <a href="/admin"><button id="back-btn">Go back</button></a>
    </form>
  </body>
</html>
