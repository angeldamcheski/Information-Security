<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Dashboard</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f5f5f5;
        color: #333;
      }
      header > h1 {
        color: whitesmoke;
      }
      header {
        background-color: #2596be;
        color: #fff;
        padding: 20px;
        text-align: center;
      }

      section {
        max-width: 800px;
        margin: 20px auto;
        padding: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        border-radius: 10px;
        border: 2px black;
        background-color: #fff;
      }

      h1,
      h2 {
        color: #333;
      }

      p {
        line-height: 1.6;
      }

      footer {
        background-color: #333;
        color: #fff;
        padding: 10px;
        text-align: center;
      }
      a {
        text-decoration: none;
        color: whitesmoke;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }

      th,
      td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }

      th {
        background-color: #2596be;
        color: #fff;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>Welcome Admin</h1>
      <p><b>Admin Dashboard - User List</b></p>
      <a href="/home">Go back</a><br />
      <a href="/logout">Click here to logout</a>
    </header>

    <section>
      <h2>Users in Database</h2>
      <table>
        <thead>
          <tr>
            <th>User ID</th>
            <th>Username</th>
            <th>Email</th>
          </tr>
        </thead>
        <tbody>
          <% if(userList.length) {%> <% for(let user of userList){ %>
          <tr>
            <td><%=user._id %></td>
            <td><%=user.name %></td>
            <td><%=user.email %></td>
          </tr>
          <% } %> <% } else{ %>
          <tr>
            No users in database
          </tr>
          <% } %>
        </tbody>
      </table>
    </section>

    <footer>&copy; 2023 Angel Damcheski. All rights reserved.</footer>
  </body>
</html>
